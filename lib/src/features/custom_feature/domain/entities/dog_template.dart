class DogsTemplate {
  final DogsSection mainSection;
  final DogsSection secondarySection;

  DogsTemplate(this.mainSection, this.secondarySection);
}

class DogsSection {
  final String title;
  final String subtitle;

  DogsSection(this.title, this.subtitle);
}